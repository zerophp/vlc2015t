<?php

function parseUrl()
{
    $request = array();
    
    // Si existe en la carpeta controllers
    // 
    
    $actions = array('usuarios'=>array('select','insert','delete', 'update'));
    
    return $request;
}